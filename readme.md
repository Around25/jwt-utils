A small package to perform jwt token operations (store, get, decode, get expiration date, check if expired, validate, remove from storage). Works in browser and React Native.

## Instalation

```
npm install --save jwt-utils
```
